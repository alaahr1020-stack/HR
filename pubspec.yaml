name: hr_guide
description: دليل الموارد البشرية - التشريعات واللوائح والإجراءات والنماذج
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  google_mobile_ads: ^9.1.0
  in_app_purchase: ^3.2.0
  shared_preferences: ^2.3.0
  share_plus: ^13.3.0
  path_provider: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/content/
    - assets/forms/
